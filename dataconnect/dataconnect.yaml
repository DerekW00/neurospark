specVersion: "v1beta"
serviceId: "neurospark"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "neurospark-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
